# コーディングルール

## 型の使用
- データの型に`any`は使わない
- 適切な型を明示的に定義する

## コード品質
- 作成後にeslintルールエラーが起きていないか確認し、起きていたら修正する
- TypeScriptの厳格な型チェックを遵守する

## 追加ルール
- 宣言されているが値が読み取られていないものは都度削除する
- 型に存在しないプロパティやメソッドの使用は禁止する
- 機能の追加や修正などでコードが肥大化しないように、適宜リファクタリングを行う
- ファンクションやメソッドはなるべく別ファイルで定義・作成しいろいろなファイルで共有できるようにする